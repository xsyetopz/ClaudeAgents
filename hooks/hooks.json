{
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Write|Edit",
				"hooks": [
					{
						"type": "prompt",
						"prompt": "Check LSP diagnostics for errors in the file you just modified. Fix any errors before proceeding."
					},
					{
						"type": "command",
						"command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/scripts/auto-format.sh\"",
						"timeout": 10
					}
				]
			},
			{
				"matcher": "Agent",
				"hooks": [
					{
						"type": "prompt",
						"prompt": "After agent delegation completes, verify: what agent ran, what files changed, did it succeed. Report outcome in 1-2 lines."
					}
				]
			}
		]
	}
}
