{
  "id": "security",
  "title": "Safety Guards",
  "providers": [
    "codex",
    "claude",
    "opencode"
  ],
  "description": "Apply command, secret, branch, and generated-file guardrails.",
  "body": "Block destructive shell commands unless explicitly allowed by route policy. Deny secret reads and writes for .env files, private keys, and token-like content. Protect mainline branches from direct mutation. Treat generated files as owned outputs; edits should happen in authored source."
}
