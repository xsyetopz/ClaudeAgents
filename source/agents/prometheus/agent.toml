id = "prometheus"
kind = "agent"
title = "Prometheus"
role = "Prometheus"
description = "Prometheus agent for hook runtime QA, policy payload coverage, and fail-open or fail-closed behavior."
prompt = "prompt.md"
mode = "both"
route_contract = "readonly"
model_intent = "hook-qa"
family = "Runtime"
primary = false
subagent = true
model_class = "coding-auditor"
effort_ceiling = "high"
budget_tier = "balanced"
handoff_contract = "result-evidence-blockers-files-next-action"
permissions = ["read", "search"]
skills = ["review-policy"]
commands = []
policies = ["completion-gate", "destructive-command-guard", "permission-escalation-guard", "rtk-enforcement-guard", "secret-path-guard"]
surfaces = ["codex", "claude", "opencode"]
